# Part 3: ANOVA, chi-square, and the decision memo

**Names:**

## ANOVA (if a numeric variable across three or more groups)

Groups and what is measured:
Conditions checked (independence, normality, spread ratio):
ANOVA table (SS, df, MS, F, F*, p):
Decision:
Tukey (only if H0 rejected): HSD and which pairs differ, as tiers:

## Chi-square (if two categorical variables)

Observed table:
Expected counts, and whether every cell is at least 5:
Categories combined (if any), and the business reason:
Chi-square, df, critical value, p:
The cell that drives the result:

If neither applies, say why here:

## Decision memo (one page, recommendation first)

**Bottom line:** one sentence.

**The decision and who owns it:**

**The evidence:** each test in one line, in business units, with its interval and p-value.

**Conditions and caveats:** what was borderline, what the data cannot support.

**What would settle what this could not:**

## AI use on this part

Tool, APA reference, what it was used for, and what you verified by hand.
