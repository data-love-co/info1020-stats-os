# Sources: your project data

- `raw/`: drop your project dataset here, any format. **Gitignored.** Nothing in this folder leaves
  your machine through this repo.
- `processed/`: the cleaned copy the skills read, with identifiers dropped or masked. This folder
  is tracked, so check it before you push a fork anywhere public.
